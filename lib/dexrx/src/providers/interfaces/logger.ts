import type { ILogger } from '../../types/logger';

/**
 * Logger provider interface for Build API
 * Uses existing ILogger interface from core
 * @category Providers
 */
export type ILoggerProvider = ILogger;
